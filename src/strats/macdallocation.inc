      parameter(IMAX=10000,JMAX=20, IPERIODS=40)
      parameter(jm=5,irdx=10,iwrx=0)
      parameter(ishort=10, fshort=50, dshort=5)
      parameter(ilong=60, flong=260, dlong=10)
      parameter(nvars=2)


  ! IMAX  : is the MAXIMUM number of bars in the market data
  ! JMAX  : is the MAXIMUM total number of properties of the simulatio
  ! IPERIODS : MAXIMUM number of periods
